<Application x:Class="StructuralOffice.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#3478F6" />
        <SolidColorBrush x:Key="PanelBrush" Color="#171C26" />
        <SolidColorBrush x:Key="BorderBrush" Color="#2B3342" />
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E8EDF7" />
            <Setter Property="FontFamily" Value="Segoe UI" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0F131B" />
            <Setter Property="Foreground" Value="#F5F7FC" />
            <Setter Property="BorderBrush" Value="#39445A" />
            <Setter Property="Padding" Value="12,9" />
            <Setter Property="FontSize" Value="14" />
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#0F131B" />
            <Setter Property="Foreground" Value="#F5F7FC" />
            <Setter Property="BorderBrush" Value="#39445A" />
            <Setter Property="Padding" Value="12,9" />
            <Setter Property="FontSize" Value="14" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Application.Resources>
</Application>
